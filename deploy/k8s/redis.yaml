apiVersion: apps/v1
kind: Deployment
metadata:
  name: llmtrace-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: llmtrace-redis
  template:
    metadata:
      labels:
        app: llmtrace-redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: llmtrace-redis
spec:
  selector:
    app: llmtrace-redis
  ports:
    - port: 6379
      targetPort: 6379
